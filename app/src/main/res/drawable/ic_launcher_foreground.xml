<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gri yarım daire -->
    <item
        android:top="24dp"
        android:bottom="48dp"
        android:left="16dp"
        android:right="16dp">
        <shape android:shape="oval">
            <stroke android:width="6dp" android:color="#888899"/>
            <solid android:color="@android:color/transparent"/>
        </shape>
    </item>

    <!-- Kırmızı nokta -->
    <item android:top="56dp" android:bottom="20dp" android:left="18dp" android:right="62dp">
        <shape android:shape="oval">
            <solid android:color="#E53935"/>
        </shape>
    </item>

    <!-- Yeşil nokta -->
    <item android:top="56dp" android:bottom="20dp" android:left="40dp" android:right="40dp">
        <shape android:shape="oval">
            <solid android:color="#43A047"/>
        </shape>
    </item>

    <!-- Mavi nokta -->
    <item android:top="56dp" android:bottom="20dp" android:left="62dp" android:right="18dp">
        <shape android:shape="oval">
            <solid android:color="#1E88E5"/>
        </shape>
    </item>

</layer-list>
